spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/yunyun
    username: yunyun
    password: yunyun
    driver-class-name: org.mariadb.jdbc.Driver

  jwt:
    secret: test

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true